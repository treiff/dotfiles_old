[user]
	name = Trevor Reiff
	email = trevorreiff@gmail.com 
[alias]
	co = checkout
        cleanup = !git remote prune origin && git gc && git clean -df && git stash clear
        lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative"
[color]
	diff = auto
	status = auto
	branch = auto

[color "diff"]
        meta = yellow 

[core]
	excludesfile = ~/.gitignore
	editor = vim --noplugin
[apply]
	whitespace = nowarn

[help]
	autocorrect = 1
[branch]
	autosetupmerge = true
        autosetuprebase = always
[push]
	default = current
[rerere]
	enabled = 1
[github]
	user = treiff
[merge]
        ff = only
